# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/

# Node (if you add tooling later)
node_modules/

# Local env / secrets — never commit your Supabase/Formspree keys
.env
.env.local

# Local editor / tooling folders
.idea/

# Private deploy notes — keep the build/host recipe out of the public repo
DEPLOY.local.md
